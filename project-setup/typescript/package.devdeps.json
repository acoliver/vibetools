{
  "_comment": "Merge these into your package.json devDependencies. Versions are minimums (^) from the source codebase; pin exact versions for reproducible CI if you prefer. Optional React/Vitest deps are listed separately.",
  "devDependencies": {
    "eslint": "^9.24.0",
    "typescript-eslint": "^8.30.1",
    "eslint-config-prettier": "^10.1.2",
    "eslint-plugin-import": "^2.31.0",
    "eslint-plugin-sonarjs": "^4.0.2",
    "eslint-plugin-eslint-comments": "^3.2.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3"
  },
  "_optional_react": {
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^5.2.0"
  },
  "_optional_vitest": {
    "@vitest/eslint-plugin": "^1.3.4",
    "vitest": "^3.2.4"
  },
  "_recommended_scripts": {
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "check": "npm run lint && npm run format:check && npm run typecheck"
  }
}
