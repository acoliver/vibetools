# Disable incremental compilation in build profiles.
# This avoids large incremental-artifact growth during local dev/test cycles
# and keeps CI builds reproducible.
# Source: jefe, luther, personal-agent (all disable incremental for dev/test).

[build]
incremental = false
